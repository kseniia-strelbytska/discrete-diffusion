seed: 1
device: auto
data:
  l: 256
  train_split: 0.9
  batch_size: 64
  grammar: anbn
model:
  max_len: 258       
  vocab_size: 6
  n_head: 4
  n_layers: 4
  embed_dim: 128
  dim_feedforward: 1024
  dropout: 0.1
  T: 500
  eos_weight: 10.0
training:
  epochs: 60000
  learning_rate: 0.001
evaluation:
  eval_every: 500
  n_samples: 100
  samples_type: random
  eval_type: autoregressive
paths:
  models_dir: models
  figures_dir: figures
  experiment_name: embed_dim=128_ffc=1024
notes: "running embed 128 with 1024 feed forward"