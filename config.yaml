seed: 42
device: cpu
data:
  l: 256
  train_split: 0.9
  batch_size: 64
  grammar: anbn
model:
  max_len: 258       
  vocab_size: 6
  n_head: 4
  n_layers: 4
  embed_dim: 128
  dim_feedforward: 1024
  dropout: 0.1
  layer_norm_eps: 0.0002
  T: 1500
  eos_weight: 10.0
  inverse_t: True
training:
  epochs: 60000
  learning_rate: 0.001
  weight_decay: 0.0
evaluation:
  eval_every: 500
  n_samples: 200
  eval_dataset: complete
  eval_type: full
paths:
  models_dir: models
  figures_dir: figures
  experiment_name: inverse_t
notes: "testing inverse t"